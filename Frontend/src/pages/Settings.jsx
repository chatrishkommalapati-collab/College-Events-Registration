import styles from './Settings.module.css'

export default function Settings() {
  return (
    <section className={styles.settingsPage}>
      <h3>Settings</h3>
      <p>This section can include notification preferences and account settings later.</p>
    </section>
  )
}
